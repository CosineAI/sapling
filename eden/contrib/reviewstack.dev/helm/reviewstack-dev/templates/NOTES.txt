1. Deploy the image built from this repo (default tag: reviewstack-dev:local).
2. Configure OAuth via values under config.data (mounted to /config.js).
3. Enable oauthService and provide GitHub client credentials to use the bundled OAuth proxy.
4. If you are using an Ingress, set ingress.enabled=true and configure hosts.
