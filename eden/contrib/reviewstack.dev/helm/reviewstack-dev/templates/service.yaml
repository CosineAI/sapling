apiVersion: v1
kind: Service
metadata:
  name: {{ include "reviewstack-dev.fullname" . }}
  labels:
    {{- include "reviewstack-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reviewstack-dev.selectorLabels" . | nindent 4 }}
